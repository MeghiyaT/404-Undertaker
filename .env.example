# Lighthouse API key for Filecoin/IPFS evidence uploads.
# This key is used ONLY by the Vercel serverless proxy (api/upload.ts)
# and is never exposed to the browser bundle.
#
# For local development:  set it here and run `vercel dev`.
# For production:         set it in Vercel Dashboard → Settings → Environment Variables.
LIGHTHOUSE_API_KEY=
